spring:
  flyway:
    enabled: false
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    diverClassName: org.h2.Driver
    url: jdbc:h2:~/quotes_test
    username: sa
    password: